# 2026-05-29 对话纪要

## 本次会话背景
上下文来自 2026-05-24 对话，本轮继续推进 GTM 商业化路径中的前端架构重构问题。

---

## 一、用户提出的前端重构需求

用户原文：
> "这样的话，等于说我目前的前端需要全部改，目前的产品我自己能用，但是需要按照产出的标准，我的想法是要能够直接让前端意见生成80%报告相关内容，所以你需要帮我想好，我这个前端优化的方向，和路径，以及prompt"

核心目标：
- 前端能自动生成 80% 报告内容
- 保持 499 交付 ≤ 1 小时人工时间
- 四个内部模块（情报看板、行业热力图、能力匹配、JD样本库）全部下沉为数据层
- 客户只看到一份报告

---

## 二、提供的设计方案

### 重构核心原则
四个模块按**信息来源**分类 → 重构为按**结论类型**分类

用户看到的只有一份报告，内部走四条数据线。

### 重构后的数据流
```
用户输入（表单）
    ↓
数据层（全自动）
    - 抓情报看板 → 公司 + 扩张信号
    - 抓热力图   → JD + 岗位画像
    - 能力匹配   → 背景 vs 画像匹配度
    - 样本库     → 共性 + 赛道差异
    ↓
主报告生成（AI）
    ↓
人工审核 + 润色（≤20分钟）
    ↓
最终交付
```

### 客户表单只问六件事
1. 你现在在什么行业 / 做哪块业务
2. 你过往的核心经验是什么（标签选择）
3. 你最想往哪个方向走
4. 你最想看哪个区域 / 市场
5. 你现在最大的困惑是什么
6. 你有没有特别想看哪类公司（可选）

### 三层 Prompt 设计

#### Prompt 第一层：数据收集 + 结构化（后台自动跑）
处理四类数据源，输出结构化摘要：
- 扩张信号：公司列表 + 信号类型 + 强度 + 来源
- 岗位画像：常见三类画像 + JD 关键词 + 进入门槛
- 能力匹配：匹配度评分 + 匹配点 + GAP 点 + 补足路径
- 赛道差异：不同赛道差异化需求 + 最低壁垒赛道

#### Prompt 第二层：完整报告生成（核心）
生成 800-1200 字报告，包含：
- 一、机会判断（300-400字）：当前市场信号 + 最值得看的3类机会
- 二、能力匹配（200-300字）：画像定位 + 匹配度 + GAP + 补足路径
- 三、下一步动作（200-300字）：第1周行动 + 第2周行动 + 能力补足优先级
- 四、特别说明（100-150字）：直接回应用户核心困惑

#### Prompt 第三层：快速审核润色
标注事实错误、信息矛盾、逻辑跳跃，给出修改建议

### 优化路径三步走
1. **今天就能做**：加「一键生成」按钮，把 Prompt 第二层做成 Master Prompt
2. **3-5天**：标准化四个模块输出格式，让数据更干净
3. **1-2周**：加审核层 + 导出优化

---

## 三、用户反馈的五条问题

### 问题1：是否应该保留现有前端，做新版本并行跑
**用户反馈：** 确认应该保留现有前端，重新做一版新逻辑。

**回应：** 对，先不动现在能跑的工具。新版本走另一条路，独立验证。

### 问题2：岗位日推作为获客钩子，不作为付费产品
**用户反馈：** 每天可以抓到潜在公司岗位，日推可能是拉客单的好办法，但定制推岗位是3999+的服务。

**回应：**
- 日推岗位 → 免费获客钩子，用来建立信任、筛选意向客户
- 每天自动抓当日新 JD → 生成简版机会速览 → 分享到群/朋友圈/newsletter
- 不需要承诺每日更新，先每周 2-3 次，看反应

### 问题3：趋势数据每周一抓一次的局限性
**用户反馈：** 现在抓的趋势其实是每周一抓了一次新闻的趋势，这个俨然是不准的。

**回应：** 两个解决路径
- 选项 A（快）：标注数据采集日期，报告中明确注明"趋势判断基于近期数据，非日更"
- 选项 B（值得做）：用定时脚本每天凌晨自动跑一次抓取，数据持续积累

### 问题4：标品优先，非标留到高客单
**用户反馈：** 
- 高度定制化对 499 开发压力巨大，数据结构需要连续很多天调整
- 499 以下的品需要核心竞争力，但标品比重应该尽可能大
- 一周的开发时间实现非标是不现实的，应该留给高客单

**回应：** 重构核心原则是**同一个报告模板，套到所有客户身上，只是变量不同**

模板结构（固定）：
- 机会判断（用不同公司/赛道填充）
- 能力匹配（用不同用户背景填充）
- 下一步动作（用不同GAP填充）
- 特别说明（用用户输入的困惑填充）

变量（表单收集，5个）：
- 用户行业
- 用户过往经验
- 用户目标方向
- 目标市场
- 核心困惑

### 问题5：用户输入到报告生成的完整数据流不清楚
**用户反馈：** 没有看到处理用户输入信息的前后端传导路径prompt，系统不知道要干什么。

**回应：** 完整数据流：
```
【表单层】客户填 5 个字段
    ↓ JSON
【后端处理层】根据用户输入，组装 Master Prompt
    ├── 把 {用户行业} → 查情报看板，取相关公司列表
    ├── 把 {目标方向} → 查热力图，取对应 JD 画像
    ├── 把 {用户经验} → 能力匹配模块，跑匹配度
    └── 把 {目标方向 + 市场} → 样本库，取赛道差异
    ↓ 结构化数据
【AI 生成层】Master Prompt + 变量 + 数据 → 报告草稿
    ↓
【审核层】你在线看报告 → 改 2-3 处 → 完成
    ↓
【交付层】复制 / PDF / 发给客户
```

技术栈（Notion + 前端）完全可以走通，需要一个**中央调度逻辑**把流程串起来。

---

## 四、下一步行动建议

现在要做的第一件事：

**在纸上把数据流图画一遍，确认每一步是什么、谁处理、什么时候发生。**

画完之后确认：
- 哪一步现在能自动跑
- 哪一步需要手动触发
- 哪一步是真正的瓶颈

然后再决定先优化哪一段。

---

## 五、关键约束再次确认

- 499 单 ≤ 1 小时人工时间
- 端到端交付时间 ≤ 60 分钟
- 其中你审核 + 润色上限 20 分钟
- 标品比例越高，开发越少，交付越快
- 核心原则：卖结论/判断/建议，不卖底层引擎